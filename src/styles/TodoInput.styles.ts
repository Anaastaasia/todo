import styled from 'styled-components';

export const Input = styled.input`
    padding: 10px;
    width: 100%;
    box-sizing: border-box;
    font-size: 14px;
    line-height: 1.5;
    overflow-x: auto;
    white-space: nowrap;
`;
